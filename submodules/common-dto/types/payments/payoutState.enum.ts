export enum PayoutStateEnum {
    CREATED = 'created',
    ACCEPTED = 'accepted',
    REJECTED = 'rejected',
    RELEASED = 'released',
    RELEASE_FAILED = 'release_failed',
    CANCELLED = 'cancelled',
}
